# Design

Tokens, typography, dashboard composition rules. Stub — the token system is not defined yet. The color rule itself lives in AGENTS.md ("Rules that never change"); this file documents the tokens once defined.

## Tokens

(not defined yet — define in `app/globals.css`, document here)

## Typography

(not defined yet)

## Dashboard composition rules

(not defined yet)
