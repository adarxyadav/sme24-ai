- 2026-08-27 — T-033 done: dashboard sidebar shell — `app/(site)/` route group now owns `SiteShell` (marketing, auth, expert, admin, design moved in; root layout keeps fonts + `ThemeProvider`), `/dashboard` renders `DashboardShell` (shadcn Sidebar offcanvas, brand → `/`, nav Your analyses / New search → `/` + role link per `profile.role`, footer `ThemeToggle` + email + Log out, `SidebarInset` as `main#main` with `SidebarTrigger` top bar); scripted Chromium 29/29 green at 1280+390 (tonal active item, offcanvas with accessible trigger, skip link first tab stop, SiteShell intact on the four other surfaces, signed-out redirect); tsc/lint/build green — found on the way: the T-006 lint boundary needed narrow exemptions (`lib/auth/get-user`, `actions/auth`) for chrome the dashboard now owns, and shadcn's `use-mobile` hook was rewritten on `useSyncExternalStore` because the repo's `set-state-in-effect` rule refuses the stock version
- 2026-08-27 — T-033 added under Now: dashboard sidebar shell (minimal) — owner opened the dashboard-refinement track; rulings: full app shell replacing marketing chrome on /dashboard, New search stays a link to `/`, expert/admin migrate later
- 2026-08-27 — T-032 done: production run `dfeb40fd` (Nestlé donor, posted by the owner in the browser — the classifier refuses session minting against prod) went queued → extracting → benchmarking → matching → generating → completed on the deployed stack: 2 web KPIs (TRIR 1.13, fatalities), benchmark verdict, proposal PDF in Storage, exactly one `parallel run created` (stage-1 cache hit, the single approved base peer call); board cleared
- 2026-08-27 — T-032 added under Now and executed to the last check: owner moved deploy up — PR stack #5→#23 merged into main via retarget-then-merge (#6 was auto-closed by a base-branch deletion and returned as #22; #20 turned out already merged; #21 the owner merged from the UI), Trigger.dev prod `20260827.2` (7 tasks) with `syncEnvVars` in trigger.config.ts pushing the four task vars from `--env-file .env.local`, Vercel `sme24-ai` live at sme24-ai.vercel.app (fra1 via vercel.json, git-connected to main, Supabase + Turnstile-test + prod trigger key env), Supabase site URL → prod domain with localhost kept in the allowlist; verified: prod / serves, magic-link reaches "Check your email", Google carries the prod callback — remaining check: one signed-in prod run on the Nestlé donor (classifier refuses session minting against prod; owner runs it by hand) — found on the way: `vercel link` rewrote `.env.local` (values kept, comments lost, `VERCEL_OIDC_TOKEN` appended)
- 2026-08-27 — fix: T-031 review — footer "For experts" now links `/#for-experts` as "Expert network" (was `/expert/apply`, which login-gates signed-out mobile visitors; the footer is the mobile path for the header nav and must carry the same destination)
- 2026-08-27 — T-031 done: `AutoHideHeader` (client, sticky, hide-on-down / show-on-up via rAF-throttled scroll compare, top-64px never hides, `onFocusCapture` reveals for keyboard, `motion-reduce` drops the transition) wraps a three-zone `SiteHeader` (brand / centered nav How it works · Expert network · Packages, hidden below `md`, footer nav as the mobile path / `AuthNav`), signed-out CTA + footer link now "Log in"; scripted Chromium 16/16 green at 1280+390 incl. reduced-motion and anchor landing, tsc/lint/build green — deviated: the signed-in check item passed on code-inspection, not live (the classifier refused loading `.env.local` into the mint-session script; AuthNav's signed-in branch is code-unchanged), owner accepted by moving to commit
- 2026-08-27 — T-031 added under Now: navbar per the owner's mockup (centered nav, "Log in" CTA) + auto-hide on scroll down / show on scroll up — owner brought the mockup and moved it up; Contact deferred to the packages/contact-form work (no destination exists yet, owner ruling)
- 2026-08-27 — T-030 done: no `cancelled` status — ruling recorded in pipeline-rules.md's decision log (only ops can cancel, the distinction already lives in the `error` text + the `run cancelled` log row, "Delayed" is honest copy for both; revisit when a user-facing cancel ships); no code
- 2026-08-27 — T-028 done: display-time derivations — `deriveFigures` in `lib/portal/ledger.ts` (recordables from TRIR × hours, lost-time from LTIFR × hours, hours from headcount × 1'880; counts from stored rows only, no chaining; contract base = `isPerMillionHours(unit)` or client origin), `getRunHeadcount` reads the one research scalar by JSON path, ledger renders ≈ + formula + `derived` badge + "Derived from …" per the now-approved design.md rule, cost card consumes derived counts with a "Derived counts used" note; verified (`t-028-spec.md`): `15687402` client TRIR 1.2 + hours 500M → recordables ≈ 600 and CHF 11'400'000, Sika `1203b16d` no duplicate beside disclosed 222, `cd7a36c2` no count row without stored hours; supersedes `t-006-spec.md` D4
- 2026-08-27 — T-029 done: BenchmarkCard insufficient-data copy drops "above" — the report order puts incident cost below the benchmark (found in the report-page mockup pass); one sentence changed
- 2026-08-27 — T-027 done: stage 3 retry no longer re-buys the peer call — the gathered result (peers, references, `parallel_run_id`, notes) is persisted as Trigger.dev run metadata + `flush()` before the judge, attempt > 1 reuses it (`peers reused` log) and metadata dies with the run so peer research stays uncached across runs; seam `FORCE_STAGE3_JUDGE_FAIL`; verified (`t-027-spec.md`): `6bc7f2bc` one `parallel run created` across a forced attempt pair, attempt 2 reused `trun_d5b71d81…` and completed through stage 5
- 2026-08-27 — report-page mockup: `context/product/design-report-page.html` (untracked like the sibling mockups) — the completed run's five sections composed as one page; signature is the findings line (masthead strip: one mono figure per section, doubling as anchor nav, gaps rendered muted), numbered chapter rules chain the pipeline order, incident cost keeps the invoice pass (rail fits a document page and 390px; bubbles would fight two tables); two states (full run, thin-disclosure run), screenshot-checked at 1280/390 — found on the way: BenchmarkCard's insufficient-data copy says "incident cost above" but the report order puts cost below benchmark, needs a copy fix
- 2026-08-27 — T-026 done: brand assets — owner's logo SVG (clean black/white paths) inlined as `BrandMark` (token fills, follows the theme) replacing ShieldCheck + text in `SiteHeader`, mark alone as `app/icon.svg` (static black/white — a favicon file has no tokens), `app/opengraph-image.tsx` via `ImageResponse` (literal cream/ink/teal per the proposal-PDF precedent); the raw files were removed from `public/` after inlining (content preserved verbatim in the components); verified: icon + og:image tags served, `/opengraph-image` 200 PNG, header screenshots light + dark with fills flipping between the two token values; tsc/lint/build green — deviated: footer keeps text-only (the lockup at footer scale duplicates the header for no gain)
- 2026-08-27 — T-026 added under Now: brand assets (owner-supplied logo into `SiteHeader`, favicon via `app/icon`, OG image) — owner moved it up; blocked until the logo file lands
- 2026-08-27 — T-025 done: theme toggle in the footer — dark mode moved from `prefers-color-scheme` media query to class-based (`@custom-variant dark` restored, tokens flip under `.dark`), `next-themes` `ThemeProvider` in the root layout (`defaultTheme="light" enableSystem` — light for every first visit, OS preference only when chosen), `ThemeToggle` (three icon Buttons, `aria-pressed`, `useSyncExternalStore` mounted guard after lint refused the setState-in-effect pattern) in `SiteFooter`; design.md + library-docs.md dark-mode rules rewritten; verified in scripted Chromium on a fresh dev server: OS-dark first visit renders light, Dark flips the canvas and survives reload, System follows OS both ways, labels + aria-pressed present; tsc/lint/build green
- 2026-08-22 — README Stack section rewritten: every integration the pipeline uses is now wired (Storage + pgvector, Trigger.dev, AI SDK + Gateway, Parallel, Turnstile, react-pdf); only Stripe remains "not wired"; Deploy still says not set up, naming the dev stack as the only environment
- 2026-08-22 — T-024 done: Turnstile on /login — Supabase captcha protection switched on (provider turnstile, secret via the management API, never in the repo), `LoginCard` renders `@marsidev/react-turnstile` with the token as a hidden field and the submit gated on it, `requestMagicLink` forwards `captchaToken` and maps `captcha_failed` to human copy; Cloudflare test keys until launch; verified (`t-024-spec.md`): Supabase refuses a tokenless OTP, the browser flow reaches "Check your email", a blanked token shows our copy — found on the way: the dev Resend sender delivers only to `service@ichotz.com` (550 for everyone else), now written into auth.md
- 2026-08-22 — T-023 done: marketing page — `/` gains How it works (five stages), What you get (the report's five cards), Packages (the four tiers from `lib/packages/tiers.ts`, prices excl. MWST, tier 4 on request, tier-3 scope caveat stated; CTA = the free report, checkout is item 14), For experts strip; header Packages link, footer nav; own metadata; verified (`t-023-spec.md`): curl signed out/in, Chromium 390/1280 px no horizontal scroll, 0 raw colours
- 2026-08-22 — T-022 done: admin surface — `is_admin()` + admin select policies on every table (`agent_logs` regains a `select` grant reachable only through that policy), `admin_list_users()` for emails, `lib/admin/read.ts` (session client), `requireAdmin()` gate, `/admin` overview, `/admin/runs` + `/admin/runs/[id]` (hidden columns + `AgentLogTable`), `/admin/users`, `/admin/experts` with `ExpertDecisionForm` → `actions/admin.ts#setExpertStatus` (service role after `getUser()` proves admin); verified (`t-022-spec.md`): client B 0 rows on `agent_logs`/others' runs/`admin_list_users`, admin A 38/38 runs + 331 log rows + 8/8 emails, other user's failed run page shows error + log, B → 307 off `/admin`, Approve/Reject move the profiles and the approved expert lands on `/expert`, replayed action as B → "Not allowed" and no change — deviated: a client's `admin_list_users()` returns 0 rows rather than an error (denial inside the function)
- 2026-08-22 — T-021 done: base → ultra escalation, both paths (`t-005-spec.md` D7 + the T-011 constraint) — `POST /api/runs` takes an explicit `processor` (form never sends it; `create_analysis_run` gained `p_processor`), stage 1 on a base run with zero numeric web KPIs flips the row to ultra, logs `escalation`, re-triggers itself with `reason: escalation` and returns; the re-run writes its own `trigger_run_id` on entry (logged), skips the claim, keeps the status, re-researches under the tier rule and carries stages 2–5; stage 3 with no numeric peer rate gathers once more on ultra; seams `FORCE_STAGE1_ESCALATE` / `FORCE_STAGE3_NO_RATES`; stage-3 ceilings raised to 1800/1500 s after a 14-min ultra peer call; verified (`t-021-spec.md`): `1572cc64` escalated once at `extracting` with a cache-hit re-run, `cd7a36c2` base → ultra peer call → 8 rated peers, default run untouched — deviated: **two ultra peer calls, not one** — `44ca6dec`'s task was cancelled when my foreground shell (which had spawned the dev worker) timed out, the worker now runs detached; a stage-3 judge retry re-buying the peer call is recorded under Later
- 2026-08-22 — T-020 done: uploaded report override (stage 1 step 4, closing `t-004-spec.md` D10) — private bucket `uploads` with no client policy, `POST /api/uploads` (session + `%PDF-` check, service-role store at `<user_id>/<uuid>.pdf`), `create_analysis_run` takes the path, trigger route accepts it only from the caller's folder and only if the object exists, `lib/upload/read.ts` reads the PDF through the Gateway as a file part into `research.upload` (never merged into the web findings, never copied by the cache), stage 2 maps upload findings ahead of web ones and writes `origin = 'upload'`, ledger shows "Uploaded report" + excerpt, search form gained the file control; verified (`t-020-spec.md`): 401/400/201 on the upload route, 400 for foreign/missing paths, `3a63d903` → TRIR 0.99 / LTIFR 0.42 / near misses 311 from the PDF over web TRIR 1.13 with fatalities still web, `7900010f` (user B, donor = A's run) has no `upload` and web TRIR 1.13 — two paid base peer calls (stage 3 ran normally), no ultra call
- 2026-08-22 — T-019 done: stage 5 — `trigger/proposal-generation.ts` chained after stage 4 (`matching -> generating -> completed`, the machine's last mover; stage 4 hands back), EHS Vault (`ehs_documents` pgvector 1536 + `match_ehs_documents`, service-role only, `lib/vault/` pins `openai/text-embedding-3-small`), `lib/packages/tiers.ts` mirroring packages.md, one `Output.object` draft (`lib/proposal/draft.ts`), `@react-pdf/renderer` PDF (`lib/proposal/pdf.tsx`) uploaded to the new private bucket `proposals` at `<run_id>/proposal.pdf`, `proposals` row with `sources`, owner storage policy + 60 s signed URL minted by the session client (`lib/portal/proposal.ts`), `ProposalCard`; verified (`t-019-spec.md`): `483ebd2b` grounded on 3 seeded vault docs → tier 3 proposal, PDF 10.7 KB fetched via the signed link and `pdftotext` shows the company name, `5aa05849` with the vault empty → `sources []` and still completed, user B 0 rows and "Object not found" on A's path; no Parallel call — deviated: PDF stylesheet uses literal colour values (a PDF has no tokens; values from design.md); the Gateway free tier rate-limits the embedding model — two attempts failed, the ≥ 60 s backoff carried the third (same key-tier limit as T-005, part of the open deploy decision)
- 2026-08-22 — T-018 done: stage 4 expert matchmaking — `trigger/expert-matching.ts` chained after stage 3 (`benchmarking -> matching -> completed`; stage 3 no longer writes `completed`), one `Output.object` call (`lib/matching/judge.ts`: risk profile, then top-3 approved experts by index with score + client-facing rationale), `replace_expert_matches` one-transaction write, `expert_matches` table with owner RLS + a second `experts` policy for matched experts, `my_expert_matches()` for the expert side, `ExpertMatchesCard` on the run page and `ExpertMatchesList` on `/expert`; four fixture experts seeded by service role; verified (`t-018-spec.md`): `5368d134` three distinct ranked matches on 4 candidates, `36f8583a` zero candidates → zero rows + honest copy, RLS A 3/3 vs B 0/0, matched expert sees company + rank, unmatched sees none; no Parallel call (stage 3 under `FORCE_STAGE3_EMPTY`) — deviated: found and fixed a chain race — a finished child left its `COMPLETED` run id on a working row until the next stage claimed, and one sweeper tick in that gap failed `0c25b89a`; stages 2 and 3 now hand the handle back to the waiting parent as their last act (rule added to pipeline-rules.md)
- 2026-08-22 — T-017 done: expert data model + surface — `experts` table (closed catalogues in `lib/experts/catalogue.ts`, owner-select RLS, no client write grants) written only through `apply_as_expert(jsonb)` (`security definer` on `auth.uid()`: upsert + `expert_status none -> pending` in one transaction, so the client-role Server Action `actions/expert.ts` stays on the session client), `/expert/apply` (form / received / not-accepted) and `/expert` (approved: status, matches placeholder, edit form), `ExpertProfileForm` + `ExpertStatusCard`, shadcn `Textarea`/`Checkbox`/`RadioGroup`, AuthNav link; verified (`t-017-spec.md`): grants denied for anon and direct client writes, RPC as user B → row + pending, scripted-browser application → row with the submitted values, redirect to "Application received", `/expert` bounced, empty submit → four field errors and no row, SQL approval → `/expert` with prefilled form, headline edit moved `updated_at`, user A sees 0 rows; approval itself stays a service-role write for the admin surface (Later)
- 2026-08-22 — T-016 done: stage 3 peer benchmarking — `trigger/peer-benchmarking.ts` chained from stage 1 after extraction (`extracting -> benchmarking -> completed`; stage 2 no longer writes `completed`), `lib/parallel/client.ts` generalised to one create/result pair with `benchmarkPeers` (base processor, never cached, public name + sector only), `lib/benchmark/judge.ts` one `Output.object` call (rate_metric, comparable peers, Hudson label, rationale, verdict), `lib/runs/rank.ts` the shared arithmetic (rank 1 = lowest rate; per-million-hours base recognised in free text; references only on that base), `benchmarks` table + RLS, `lib/portal/benchmark.ts` re-deriving rank, `BenchmarkCard`; verified live (`t-016-spec.md`): `cdfb825f` rank 2 of 4 on TRIR / calculative with 5 cited peers, `5ba1a856` a second distinct peer call while stage 1 hit cache, `a13d4506` insufficient-data path under `FORCE_STAGE3_EMPTY`, pathological → "Emerging", user B 0 rows / anon denied — **two paid Parallel base calls** (peer research is never cached by contract); worker on `PIPELINE_MODEL=openai/gpt-5-mini` — deviated: references on another base are now withheld from the judge too, after the first run's rationale cited a median the card had dropped
- 2026-08-22 — T-015 done: annual incident cost in the read layer — `lib/portal/incident-cost.ts` (four constants citing the ISO 45004 docx, the contract's severity waterfall: fatalities flat, lost-time min–max, remaining recordables floored at zero; absent count → absent row; lost-time unknown → every recordable at the recordable row) and `IncidentCostCard` on the completed run page; verified by curl (`t-015-spec.md`): `a66d4e83` (Nestlé cache hit, client 10/3/0) → CHF 273'000 – 369'000 with three rows, `879ae160` → CHF 2'400'000 + "not priced" note, `d7fd0dc7` → CHF 2'250'000 + lost-time-unknown note, failed run shows no card — deviated: the Later gate ("source doc in context/product/") read as already met, the docx is there; design.md gained the proposed ≈ + formula rendering rule for T-016, awaiting the owner's OK
- 2026-08-22 — T-014 done: live run progress — `components/dashboard/RunProgress.tsx` (client) calls `router.refresh()` every 5 s while the run is queued/in progress, so the page re-reads through `lib/portal/` and swaps to the terminal block + ledger in place; no new route, no realtime (it would need the engine's run id, which the dashboard may not know), copy no longer says refresh; verified in scripted Chromium (`t-014-spec.md`): `eb1b8c69` Nestlé cache hit went Queued → In progress → Completed with the ledger and a `window` marker intact (no reload), terminal HTML has no poller, client bundle has 0 hits for `trigger_run_id`/`@trigger.dev`
- 2026-08-22 — T-013 done: `parallel run created` is logged before the result wait — `researchCompany` gained `onRunCreated`, fired between the create call and the blocking result loop, and stage 1 writes the log row there, so a run that dies mid-wait names the paid Parallel run instead of showing `cache miss` and nothing else (found in T-011); verified live (`t-013-spec.md`) on `d7fd0dc7` Geberit AG `geberit.com` — **one paid ultra call, a deliberate cache miss because the row under test exists only on a miss**, chosen as a new Swiss donor: the row (`trun_68d2b2d6…`) was read at 07:46:47 while the run was `researching` with `research` null, `research stored` followed 11 min later, run `completed` with 3 web KPIs on `PIPELINE_MODEL=openai/gpt-5-mini`
- 2026-08-22 — T-012 done: a stage-1 retry keeps its claim — the `queued -> researching` claim in `trigger/company-research.ts` is now `queued OR (researching AND trigger_run_id = own run id)` in the same single update, so attempt 2+ of the run that already holds the row re-enters the work instead of logging `run already claimed` and returning with the retry budget spent (found in T-011); `FORCE_STAGE1_RETRY` seam (`1` = attempt 1 throws after the claim, `always` = every attempt); verified live (`t-012-spec.md`): `c5b57238` completed on attempt 2 with no claim loss, a foreign `start` trigger against hung `32511048` still exited `skipped` with one `run already claimed` row, `c0d681c2` ended `failed` via `onFailure` after three post-claim throws — all Nestlé `nestle.ch` cache hits, no Parallel call; dev worker ran with `PIPELINE_MODEL=openai/gpt-5-mini` (Gateway free tier, as in T-005)
- 2026-08-22 — T-011 done: every working status after `queued` has an owner — `trigger/sweep-stalled-runs.ts` (`*/5` cron) asks `runs.retrieve` whether the Trigger.dev run named by the new `analysis_runs.trigger_run_id` (written inside stage 1's and stage 2's claims, so the child decides at `extracting`) is still alive, and sets `failed` + error + one `agent_logs` row when it is dead (crashed, cancelled with no worker, returned without a terminal) or when a working row has no handle at all; no age threshold, every SDK status mapped explicitly, uncertainty (thrown/404 retrieve — another environment's run) never fails a run, and the write is guarded on the working status and the handle it asked about; verified live (`t-011-spec.md`): SIGKILLed attempt → `CRASHED` → `failed` in one sweep, alive runs (Parallel wait, `triggerAndWait` child) untouched across 3 sweeps, 11 terminal rows byte-identical, dashboard page clean — deviated: `1d3d2bed` was requeued by hand in another session at 06:46 before the sweeper existed and completed on its own, so its clause is covered by the no-handle fixture instead; "deployed" = the dev-environment worker, the only environment that exists; three Nestlé test runs posted with `nestle.com` (donors are `nestle.ch`) each cost a Parallel ultra call before cancellation — my error, noted under Later with the late `parallel run created` log
- 2026-08-21 — consistency pass before T-003: `first()` (duplicated verbatim in the login and confirm pages) extracted to `lib/search-params.ts` ahead of T-003 becoming its third caller; the false "new tables are not auto-exposed" comment corrected in `create_profiles_and_role_lock` too (its grants are load-bearing there — they follow a `revoke all` — only the reasoning was wrong); architecture.md now prescribes the `revoke all` + re-grant idiom by name and lists the fourth migration. Root cause of the anon gap named: `create_profiles_and_role_lock` used `revoke all` then re-grant, `create_analysis_tables` used a partial revoke listing only writes — same day, two idioms, and only the partial one leaked the default `select`. Audit found no hardcoded colors, no stale registry entries, no `any`; tsc + lint + build green

- 2026-08-21 — reverted the uncommitted `grant` edit to the already-applied `create_analysis_tables` (no-op grants on an applied file `db push` would never re-run) and replaced it with migration `20260821090607_revoke_anon_select_on_analysis_tables`; architecture.md Auth/RLS rule inverted to match Postgres — the previous entry had it backwards: `pg_default_acl` grants `anon`/`authenticated`/`service_role` every privilege on new `public` tables, so `authenticated` already held `select` and the owner-read policies were never unreachable (the throwaway-Postgres check reproduced a condition Supabase does not have). Real finding underneath: the original revoke listed only write privileges, leaving `anon` its default `select` on `analysis_runs`/`kpis` — no leak, since the policies are scoped `to authenticated` and `anon` reads 0 rows, but the boundary rested on RLS alone. Verified against the linked DB, not a local stand-in — pushed and confirmed: `anon` now holds no privilege on either table, `authenticated` keeps `select`, `service_role` keeps CRUD

- 2026-08-21 — migrations restamped to real UTC times and renamed verb-led (`20260820114500_create_analysis_tables`, `20260820123014_create_profiles_and_role_lock`, `20260820171022_backfill_missing_profiles`), remote ledger repaired to match; library-docs now requires `supabase migration new` with verb-led names instead of hand-written noun-led prefixes — invented round-hour timestamps collide across branches and noun names don't say what ran; schema verified identical and `db push --dry-run` reports up to date

- 2026-08-20 — T-009 done: passwordless auth end to end — `profiles` + `handle_new_user` trigger + role lock (service-role-only writes, proven by SQL), magic link via `/auth/confirm` token-hash page, Google via `/auth/callback`, `/auth/redirect` role dispatcher, `safe-next` + `get-user` helpers, proxy session gating; T-002 password flow (AuthForm, /signup, signup/login actions) deleted, no compat layer — deviated: Resend SMTP configured now rather than at launch (Supabase locks email-template editing behind custom SMTP and the token-hash link needs an edited template), a backfill migration was added for users created before the trigger existed, and the proxy now skips non-GET requests so Server Action POSTs reach their page

- 2026-08-20 — T-002 done: Supabase EU project linked, migration 20260820000000_analysis_slice (analysis_runs, kpis, agent_logs; enums; RLS owner-select only, writes + agent_logs revoked for anon/authenticated) pushed to a fresh hosted DB, SQL proof green (owner 1/1, second user 0, anon 0, non-service writes denied, service_role writes ok); email+password signup/login/logout via actions/auth.ts, AuthForm, AuthNav, proxy.ts session refresh; library-docs Supabase section, architecture Data + Auth/RLS, ui-registry, README, .env.example — browser round-trip confirmed by user; password flow is interim, T-009 replaces it
- 2026-08-20 — auth.md written (passwordless magic link via token-hash confirm page + Google, profiles with locked roles, getClaims proxy gating, safe `next`) and T-009 added under Next after T-002 — user brought the old product's auth design; review found cross-device/link-scanner breakage, open redirect, role-lock window and applied-vs-approved leak, all fixed in the contract

# Log

One line per change, newest first: `YYYY-MM-DD — what — why` (append `deviated: <how>` if it diverged from the ticket).

- 2026-08-22 — T-006 done: dashboard read layer — `lib/portal/` (`listRuns`, `getRunKpis`, `buildLedger`, `runState`: nine `run_status` values → five render states via an exhaustive Record), `/dashboard` run list + `/dashboard/runs/[id]` detail with the KPI ledger (seven contract rows, "Not disclosed" for absent web metrics, `lost_time_injuries` only when client-supplied, "Client-provided" in the Source column), five-state `RunStatusCard` whose failed copy reads nothing but the status, `app/dashboard/not-found`, shadcn Table + Badge, `/auth/redirect` client target → `/dashboard`; the read-layer boundary is an ESLint `no-restricted-imports` rule (dashboard → `lib/portal/` only, `lib/portal/` → session client + `metrics.ts` only) proven firing on four injected imports; verified live with curl + minted sessions — `2a456942` completed, `268cca06` Delayed with zero internals in the HTML, `3b72bdd5` no_data, `879ae160` queued, `1d3d2bed` in progress, `8e6facc4` three Client-provided rows, user B on `2a456942` → 404 — no derivation computed (Later), no polling (Later)

- 2026-08-22 — AGENTS.md registry rule scoped to "when a component is added, changed, or removed" — a T-005 review read the unconditional "update `ui-registry.md`" as requiring an entry for a backend-only change; the registry's own header already carries that condition, the rule now says it too

- 2026-08-22 — T-005 done: stage 2 KPI extraction — `trigger/kpi-extraction.ts` chained from stage 1 via `triggerAndWait` (a child failure is logged, never rethrown, so a model failure cannot buy a second Parallel call), `lib/extraction/` (Zod enum derived from `CANONICAL_METRICS` minus `lost_time_injuries`; the model names a finding per metric and code copies value/unit/period/url/excerpt/confidence from it, so no figure is ever retyped), `replace_extracted_kpis` Postgres function (one transaction: delete `origin <> 'client'`, anti-join insert, so client rows keep their ids across retries), `ai` + AI Gateway with `PIPELINE_MODEL` override; verified live on Nestlé cache hits — `2a456942` completed with TRIR 1.13 + fatalities 2 cited verbatim, `8e6facc4` kept three client rows byte-identical through a forced retry and the function refused a web TRIR over the client's, `268cca06` failed on an injected `headcount` with zero rows written anywhere, and the failure path proved itself unprompted on `1e50358b` — deviated: the Gateway key is free-tier and refuses every Anthropic model, so the live checks ran on `PIPELINE_MODEL=openai/gpt-5-mini` (code still says `anthropic/claude-sonnet-5`); the base→ultra escalation is left under Later (no surface can create a base run, and it lives in stage 1 — `t-005-spec.md` D7)

- 2026-08-21 — T-010 follow-up: two holes closed in the duplicate guard, both found in PR review — the claim was gated on `if (run.status === "queued")`, so a sweeper re-trigger arriving after the run had moved on skipped it and ran a second paid Parallel call over good output (the claim is now attempted for every `reason: "start"` trigger and only losing the conditional update counts, with escalation re-runs exempt via an explicit payload reason); and the sweep ceiling counted sweeps rather than enqueue failures, so a second concurrent search waiting behind its owner's concurrency-1 queue was marked `failed` on the fourth sweep while the task ahead of it was still running — verified live: a trigger against an already-`researching` run leaves `research` null, a healthy waiting run survives 5 sweeps, a run with 3 recorded enqueue failures still terminates

- 2026-08-21 — run `91c61651` (Nestlé S.A.) manually set `failed` and the stalled-in-progress gap added under Later — it had been stuck `researching` since 13:58 from T-004 testing, holding that user's concurrency-1 slot and blocking T-010's live checks; the same stall class T-010 fixes, but on a status the new sweeper deliberately does not own, since a run legitimately sits in `researching` for up to `maxDuration` and only the Trigger.dev API can say whether the task is still alive

- 2026-08-21 — T-010 done: `queued` runs are self-healing — the trigger route's enqueue is now best-effort (201 with the runId either way, a failed enqueue logged to `agent_logs` instead of a 500 that stranded the committed run and invited a duplicate resubmit), `trigger/sweep-queued-runs.ts` re-triggers runs still `queued` past 5 minutes on a `*/5` cron and terminates one after 3 fruitless sweeps, stage 1 claims its run with a conditional `queued -> researching` update, and a partial index on `(created_at) where status = 'queued'` serves the scan — `queued` was the only status with no owner: `onFailure`/`onCancel` both require the task to have started, so a failed enqueue, a `PENDING_VERSION` deploy skew or a worker dying pre-attempt left a run waiting forever; verified live — a forced enqueue failure returned 201 and the run was recovered by the sweeper with no duplicate row, two concurrent triggers on one run left exactly one worker past `queued`, and a permanently unenqueueable run ended `failed` with the error column and one log row after exactly 3 sweeps; supersedes `t-004-spec.md` D2 — deviated: the DB claim, not Trigger.dev's `idempotencyKey`, is the duplicate guard, because that store clears a failed run's key and expires at 30 days, so it cannot be the authority on whether our work started

- 2026-08-21 — T-004 done: stage 1 company research — Trigger.dev v4 installed (project sme24-ehs, eu-central-1), `trigger/company-research.ts` with per-user queue concurrency 1, `lib/parallel/` (EHS JSON schema + Zod mirror, blocking-GET result loop, no webhook), `lib/runs/research.ts` (cache lookup + tier rule + no_data predicate), `lib/runs/agent-log.ts`, and the trigger route's `tasks.trigger` call; verified live against the real stack — Nestlé stored 40 findings with basis[] citations and NACE 10.89, a second run by a different user hit cache with no Parallel call, a forced failure wrote failed+error+log, and a Swiss SME with no disclosure ended no_data — deviated: the uploaded-PDF override (stage 1 step 4) is scoped out to a Later ticket, since no Storage bucket exists and no Check condition covers it; also added an `onCancel` hook and threaded the run's abort signal into the Parallel fetches after a stalled worker showed cancelled runs strand at `researching` (onFailure does not fire on cancellation)

- 2026-08-20 — T-008 done: tokens re-tuned to the Perplexity direction (cream canvas, warm ink and stone, teal #016a71 / #00a1ac as the only accent, success/warning/info status tokens added, weights capped at 500, tracking normal except hero); design.md Tokens + Typography rewritten, /design shows status tokens; all pairs ≥ 4.5:1 both modes — user chose the reference after comparing teal, muted purple and high-chroma violet

- 2026-08-20 — T-007 done: /design reference page (all token pairs, lines, chart ramp, radius, type scale, Button variants × sizes; unlinked, noindex) — tokens were only visible as a table; deviated: dark sidebar-primary fixed from shadcn's leftover blue to the neutral value design.md already specified, found by the page

- 2026-08-20 — T-001 done: semantic token system in app/globals.css (shadcn CSS vars, OKLCH, one teal brand hue, dark via prefers-color-scheme), shadcn init (radix-nova, neutral, Button), app shell (SiteShell = SkipLink + SiteHeader + main + SiteFooter) replacing the starter page, starter SVGs removed; design.md Tokens + Typography, ui-registry, library-docs (Tailwind + shadcn sections) filled — first feature needs a token system before any surface is built

- 2026-08-20 — kpi-contract.md corrected on user rulings (TRIR is per 1'000'000 hours, same base as LTIFR; loss model reverted to the ISO doc's per-injury table — fatality 1'200k flat, LTI 56–88k, remaining recordables 15k, HCI-burn and first aid unpriced) and packages.md seeded from the client's annotated comparison slide; glossary + README call tier 4 priced-on-request, not a retainer — the additive loss ranges came from a deck we are not carrying over, so the cited source had to be the controlled doc in this repo; packages seeded ahead of its ticket at the user's request

- 2026-08-20 — first feature scoped into tickets.md (T-001→T-006: the funnel spine — company name in, extracted KPIs with provenance out; stages 1+2 only, signup gated) and T-000 executed: kpi-contract.md rewritten from the client's ISO 45004 doc (all 38 indicators with web/client source tiers, the canonical 7 ask set, derivation formulas, absolute-cost loss model), pipeline-rules.md loss wording corrected, glossary gained lagging indicator / leading indicator / annual incident cost — the seeded five-metric list omitted the counts and hours the CHF figure needs, and "gap→CHF" contradicted the client's own methodology

- 2026-08-17 — pipeline-rules.md audited (4-angle review) and rewritten: contradictions fixed (call count, cache-vs-always-run, model string), cache mechanics + key normalization + upload home + failed-setter + escalation status defined, ported code names dropped, decision log deduped to dates+rationale; kpi-contract.md seeded with canonical+derived metrics, library-docs.md seeded (AI SDK/Gateway, Trigger.dev, Parallel), architecture.md stubs now point at the contract, README CHF claim corrected — audit found the port carried old-product residue and contract gaps

- 2026-08-17 — pipeline-rules.md rewritten from the old product's proven contract (Parallel ultra, live peer benchmarking + comparability rules, AI matching, CHF in code at display time, optional client KPIs, proposal + vault in v1); glossary + README updated to match — old contract's user-confirmed decisions reviewed one by one and re-confirmed, superseding yesterday's from-scratch draft
- 2026-08-17 — pipeline-rules.md written (stages, provenance, failure handling, re-runs) + Intake/Provenance added to glossary — contract must exist before first pipeline stage; decisions: intake upfront, hybrid benchmarks, auto-publish, deterministic matching (superseded same day, see above)
- 2026-08-16 — README rewritten (product, stack, run, deploy stub) and CONTEXT.md glossary seeded with core terms — closes the last stale pointer in AGENTS.md "Read first"
- 2026-08-16 — "terse over grammatically complete" moved from Coding behavior to last bullet of Rules that never change — always-on rules live in Rules, chronological sections stay code-only
- 2026-08-16 — AGENTS.md reordered: Coding behavior moved before Engineering principles, ticket gate now leads Rules that never change (color rule moved last) — sections follow the chronology of a work session
- 2026-08-16 — context/ layout settled: recurring file kinds at context/ root, SME24-only docs in context/product/ (renamed from project/ — "product" states what doesn't carry over between projects); AGENTS.md pointers updated — flat root beat a fuzzy common/ label
- 2026-08-16 — AGENTS.md cleanup: deduped color rule to single source, merged overlapping library principles, marked missing dirs as target state, library rule no longer requires uninstalled skills — fix defects found in doc review
- 2026-08-16 — scaffolded context/ doc system as stubs — make AGENTS.md workflow followable from the first ticket
2026-08-20 — app/layout.tsx: suppressHydrationWarning on <body> — browser extension (ColorZilla) injects cz-shortcut-listen attr, causing hydration console error
2026-08-21 — T-003: search form + trigger route (`app/api/runs`), `cacheKey()`, `create_analysis_run` migration, service-role client, `lib/portal/` read layer, run page — the funnel's entry point; both writes go through one Postgres function so a queued run can never lose the client KPIs it was started with
