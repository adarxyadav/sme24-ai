# Pipeline rules

The AI pipeline contract: stages, inputs/outputs, failure handling. Stub — write the contract before building the first pipeline stage; pipeline code follows this file, not the other way around.

## Stages

(not defined yet)

## Failure handling

Pipeline errors go to `agent_logs`, never the UI.
